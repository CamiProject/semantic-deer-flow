version: "1"
tables:
  demo_sites:
    access: scoped
    scope_dimension: site
    scope_column: id
    allowed_fields: [id, name]
  demo_projects:
    access: scoped
    scope_dimension: site
    scope_column: site_id
    allowed_fields: [id, name, site_id]
